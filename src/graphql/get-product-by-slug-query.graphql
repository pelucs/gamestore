query GetProductBySlug ($slug: String) {
  product(where: {slug: $slug}) {
    id
    name
    price
    slug
    brand
    category
    description
    departament
    image {
      url
    }
  }
} 