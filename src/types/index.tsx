export interface IProducts {
  id: string;
  name: string;
  price: string;
  slug: string;
  brand: string;
  category: string;
  description: string;
  departament: string;
  image: {
    url: string;
  }
}